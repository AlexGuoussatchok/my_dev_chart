import 'package:flutter/material.dart';

class FilmsCatalogueScreen extends StatelessWidget {
  const FilmsCatalogueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Films Catalogue'),
      ),
      // Add your films catalogue content here
    );
  }
}